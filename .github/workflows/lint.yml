name: Lint

on:
  workflow_call:

jobs:
  setup:
    uses: ./.github/workflows/setup.yml
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Sjekke opp mot Lint-regler
        run: yarn run lint
