name: Lint

on:
  workflow_call:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: setup
        uses: ./.github/workflows/setup.yml
      - name: Sjekke opp mot Lint-regler
        run: yarn run lint
